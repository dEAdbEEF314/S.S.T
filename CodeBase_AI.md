# S.S.T (Steam Soundtrack Tagger) - AI向けコードベース情報ドキュメント

## プロジェクト概要

**S.S.T (Steam Soundtrack Tagger)** は、Steamで購入したサウンドトラックを自動的に識別し、情報が不足しているメタデータ（曲名、アーティスト名など）を外部データを用いて補完・タグ付けする高精度なスタンドアロンCLIツールです。DJ機材などでの視認性を考慮したメタデータのクリーニングも自動で行います。

### 🚨 最重要ルール (Strict Rule)
- **Steamライブラリの読み取り専用 (Read-Only)**: 元のSteamライブラリ内のファイルは絶対に書き換え、削除、移動しません。すべての変換・タグ付け結果は指定された出力先 (`SST_OUTPUT_DIR`) にZIPアーカイブなどとして書き出されます。
- **CHANGE_HISTORY.md の更新**: ワークスペース内のコードやファイルに変更を加えた場合（テスト前のシステムクリーンアップを除く）、必ず `CHANGE_HISTORY.md` の末尾に変更履歴（日時、ファイル名、変更内容を日本語で）を追記する必要があります。

### 🛠️ 技術スタック
- **言語・環境**: Python 3.12 (パッケージ管理: `uv`)
- **外部ツール**: FFmpeg (音声フォーマットの変換に使用), SQLite3 (ローカルのデータ・キャッシュおよび状態管理)
- **AI/LLM**: 大規模言語モデル (GeminiなどのクラウドAPI、またはOllamaを用いたローカル推論) をメタデータの推論と統合に使用。

### 🚀 システムアーキテクチャ (Two-Phase Pipeline)
システムは **「ハイブリッド・エッジ処理」** を採用しており、ネットワークI/O処理とLLM推論を明確に分離することで処理速度を最適化しています。

1. **スキャン (`scanner.py`)**: 
   ローカルのSQLiteデータベースを参照し、Steamライブラリから未処理のアルバムを特定します。
2. **フェーズ1: データ事前一括取得 (`prefetcher.py` / `ident/` など)**: 
   LLMを待たせることなく、音声指紋 (`fpcalc`) の計算と外部API (AcoustID, MusicBrainz) からのメタデータ取得をマルチスレッドで一括実行し、ローカルDBにキャッシュします。
3. **フェーズ2: LLMによる推論・統合 (`llm.py` / `virtual_album.py` など)**: 
   Steam公式データ、ローカル実体ファイル、音声指紋データ、テキスト検索結果からなる「4つの仮想アルバム」を構築します。LLMはこれらを比較し、Steam公式データを「絶対的な正本」としつつ不足分を補完する堅牢な推論を行います。
4. **パッケージ化と出力 (`packager.py`)**: 
   推論結果に基づき音声を変換、ID3v2.3タグを付与し、新しいファイルとして出力ディレクトリに保存します。不明瞭な結果となった場合は `review` ディレクトリに隔離され、手動確認（監査レポート付き）に回されます。

### 📂 主要なディレクトリ構成
- **`src/sst/`**: アプリケーションのコアロジック。スキャン、LLM連携、仮想アルバム構築、VRAM管理 (`vram_manager.py`) などのモジュール群が含まれます。
- **`docs/`**: 詳細な仕様書、アーキテクチャ、タグ付けルール (`TAGGING_RULE.md`)、運用ガイドなどが格納されています。
- **`data/`**: ローカルのSQLiteデータベース、キャッシュ置き場。
- **`tests/`**: テストコードや一時的な検証用スクリプトの配置場所。手動検証などのスクリプトもここに置くルールになっています。

### ✨ 特筆すべき機能
- **三権分立ロジック**: 設定 (`.env` = 立法)、推論 (LLM = 司法)、実行 (Python System = 行政) という役割分担。
- **動的VRAMスケジューリング**: ローカルLLM (Ollama) 使用時、空きVRAMを自律的に検出し、並列処理の数を動的に最適化する「Token Stingy戦略」を搭載。
- **タグの自動調整**: DJ機材の視認性を高めるための「Dirty Tags（例: `01. Title` のようなトラック番号入り）」のクリーニングや、文字数制限に合わせたタグのプルーニング（切り詰め）。
